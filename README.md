# Project Setup Instructions

## Prerequisites
- Node.js
- Maven

## Installation
- Clone the repository
- Run `npm install` for frontend dependencies
- Use `mvn install` for backend dependencies

## Running the Project
- Use `npm start` to start the frontend
- Use `mvn spring-boot:run` for the backend